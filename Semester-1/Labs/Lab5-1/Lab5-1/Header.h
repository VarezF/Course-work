#include <stdio.h>
#include <math.h>

double get_values(FILE *infile);

double calculate_avg(double sum, double counter);

void print_to_file(FILE *outfile, double avg);

int greatest_common_divisor(int value1, int value2);


